namespace Backstage
{
    /// <summary>
    /// Marker interface for Domain events.
    /// Domain events are synchronous events that are performed inside the current context.
    /// </summary>
    public interface IDomainEvent
    {
    }
}
